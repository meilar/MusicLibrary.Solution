@{
  Layout = "_Layout";
}
  @using MusicLibrary.Models
  <h1>Artists</h1>
  @if (@Model.Count == 0)
  {
    <h3>No Artists have been added yet! Go add one!</h3>
  }
  @foreach (Artist artist in Model)
  {
    <h3><a href='/artists/@artist.Id'>@artist.Name</a></h3>
  }

